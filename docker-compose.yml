version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: control_cash_backend
    container_name: control_cash_backend
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
    env_file:
      - .env
    restart: always
