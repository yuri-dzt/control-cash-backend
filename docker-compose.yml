version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: framefree_backend
    container_name: framefree_backend
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
    env_file:
      - .env
    restart: always
